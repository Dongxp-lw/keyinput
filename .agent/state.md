# 当前状态

最后更新：2026-06-27。**接续工作先读本文件。**

## 阶段

- 文档阶段：完成（产品/技术/版本/测试/11 份实现文档/开源组件选型）。
- L0 纸面设计：完成（工程基础、模块架构、任务板 L0–L6）。
- **开发阶段：进行中**。共享核心（L2/L3）+ FFI 表面（L2-06）全部完成；**L4 Android MVP 全部落地（编译验证；运行期待验证）**。

## 进行中

- **Android MVP 全部落地且补齐（含二维码扫描 D-014），`:app:assembleDebug` 绿**。**测试阶段推进：L6-01 KAT（88/88）、L6-02 离线/隐私（静态+运行期大部分）、L0-05e CI（workflow+本地门）、L6-03 运行期冒烟覆盖核心流程 均已完成**。**API 36 模拟器冒烟实测通过**：安装→启动（native .so 加载无崩溃）→创建保险库（Argon2id 落盘）→主密码解锁（加解密往返成立）→口令 0 次入日志→StrictMode 实触发→TOTP 实时码逐位正确→复制遮蔽+自动清除→**安全键盘 IME 端到端（自绘键盘渲染→独立 Argon2id 解锁→选条目/字段→`commitText` 直填，TOTP 直填码逐位正确）**→**改条目+落盘（GitHub2→GitHubEdited，锁定后生物识别解锁仍在）**→**生物识别解锁（L4-BIO）端到端**。**发现缺陷并已修复验证（导出/导入）**：自动锁定与自家 SAF 选择器冲突导致导出写 0 字节——已修（开选择器前置位一次性抑制后台锁定标志）并重装实测通过：导出 1094B 加密包（无明文）+ 导入整库恢复（标题/TOTP 种子逐位还原）。**删条目+落盘**（软删除→列表空→锁定/生物识别解锁后仍空→再导入恢复）。**二维码扫描集成**（CAMERA 运行时授权→zxing CaptureActivity 相机预览启动，本进程不误锁；实际解码无法喂虚拟相机故未测）。剩余运行期：剪贴板 30s 实际清除（adb 读不到剪贴板，已由遮蔽+提示+代码佐证）、二维码实际解码、真机抓包（但**无 INTERNET 权限**已是更强的网络保证）+ L6-04（v1.0）。

## 下一步

- **运行期验证（部分完成）**：已过（API 36 模拟器）：创建→解锁→增条目→搜索→TOTP 实时码→复制(遮蔽)→**安全键盘第三方直填（不过剪贴板，`commitText`，TOTP 逐位正确）**。**未验证（需设备/更多操作）**：改/删条目、生成密码弹窗、剪贴板 30s 实际清除、导出/导入 SAF、后台自动锁定、生物识别启用+解锁、二维码扫描（相机）、真机抓包零流量。
- **L6 剩余**：L6-03（v0.2 退出标准回归，**运行期门**）、L6-04（发布门禁 MASVS，属 v1.0）。已完成：L6-01（KAT）、L6-02 静态部分、L0-05e CI（本地门验证，真实 runner 待跑）。
- 已锁定 D-011（minSdk 28）。D-012 暂定：应用 ID `com.lincdkeyinput`（工作值，发布前定稿）；产品**不**整体改名，文档仍称 Private Input Vault。

## 已确认（用户 2026-06-24）

- 架构锁定：**一套 Rust 共享核心 + 各端原生 UI**（D-006）；加密走 Rust crate（D-004）。已写入 [技术架构](../docs/technical/architecture.md) §2。
- 商业模式：免费层保证正常使用（密码、2FA、本地保险库、导出与手动迁移）；增值（云同步、键盘皮肤）账号绑定、延后 MVP、不影响正常使用（D-010）。已写入 [版本与权益计划](../docs/product/version-plan.md)。
- 开发阶段已授权（2026-06-24）：同意安装工具链并开始落地（“你可以直接安装，我没有意见”）。
- 工作区 `.agent/` 采用（D-009）。

## 开发环境（本机已装，2026-06-24）

- Rust：stable **1.96.0**，host **x86_64-pc-windows-gnu**（`rust-toolchain.toml` 暂用 `stable`，精确版推迟到 CI）。cargo/rustc 在 `%USERPROFILE%\.cargo\bin`（已入用户 PATH）。
- MinGW-w64：**WinLibs POSIX/MSVCRT**（winget `BrechtSanders.WinLibs.POSIX.MSVCRT`），提供 `dlltool.exe`——gnu host 编译 `windows-sys`（uniffi 依赖树）必需；bin 已入用户 PATH。
- Android SDK：`%LOCALAPPDATA%\Android\Sdk`（build-tools 35/36、platform android-35、platform-tools、emulator）；**无 cmdline-tools/sdkmanager**；`ANDROID_HOME`/`ANDROID_NDK_HOME` 已设。
- Android NDK：**r27d（27.3.13750724）**，SHA-1 校验通过，装于 `…\Sdk\ndk\27.3.13750724`（= `ANDROID_NDK_HOME`）。
- Rust Android targets：aarch64/armv7/x86_64/i686-linux-android 已装；`cargo-ndk 4.1.2` 已装。
- Gradle：8.9（SHA-256 校验）解压于 `%USERPROFILE%\gradle-dist`（仓库外）；工程 wrapper 已生成。AVD 已有（Medium_Phone_API_36 等），WHPX 加速可用，可跑 instrumented 测试。
- JDK：Oracle 17 + Microsoft 21（Gradle 实跑用 21）；git 已装。
- 说明：host 工具链仅用于本机开发/测试核心；Android 交叉编译用 NDK clang（与 gnu/MinGW 无关）。

## 待验证（不阻塞当前）

- HarmonyOS 的 Rust 目标工具链可行性 spike（Node-API 路径已确认）。

## 最近完成

- **启动期主线程 IO 修复（StrictMode，2026-06-27）+ Dependabot AGP 锁定**：之前运行期冒烟发现的「ViewModel.init 主线程磁盘读」已彻底修复并实测 **0 violations**。`VaultStore`/`BiometricGate`/`UnlockThrottle` 的 `filesDir`/文件解析改 `by lazy`（构造不碰盘）；`VaultViewModel` 惰性持有 `VaultManager`，把首次构造（含 native `.so` 加载）+ vaultExists/throttle/biometric 读取放进后台协程；解锁屏的生物识别可用性改用 `biometricEnabled` StateFlow + 非磁盘的硬件查询；`unlock()`/退避倒计时的 throttle 读写包进 `Dispatchers.Default`。API 36 实测：启动→解锁屏正确、生物识别解锁→列表正确、**启动期 StrictMode 磁盘违规 0**（原先多处）。**已知遗留（已记，未修）**：生物识别解锁回调 `BiometricGate.unlock` 仍在主线程读 `bio.bin`（BiometricPrompt 回调在主线程；属安全敏感流程，需谨慎重构；DEBUG-only StrictMode，不影响功能/发布）。另：`dependabot.yml` 加 AGP 大版本 ignore（D-013 锁 8.7.x），冲突的 AGP 9.2 PR 应关闭。提交 `14ccdc1`/`f532443`/`dfdf32a` 已推送 `origin/main`。2026-06-27。

- **工程治理规范集落地（用户 2026-06-27，D-016/D-017）**：补齐主流治理文件，使项目更可控、质量更高。**许可证 = Apache-2.0**（开源 + 免责/责任限制 + 修改版须标注改动，不对第三方修改版负责；与依赖许可证兼容）。新增：根级 `LICENSE`、`NOTICE`（第三方组件，以构建清单为权威）、`CONTRIBUTING.md`（Conventional Commits + DCO sign-off + 分支/PR/安全红线）、`SECURITY.md`（私密漏洞披露）、`CODE_OF_CONDUCT.md`（Contributor Covenant 2.1）、`CHANGELOG.md`（Keep a Changelog，含 0.2.0 与 Unreleased）、`AGENTS.md`（AI/协作工作约定）、`.editorconfig`、`deny.toml`（cargo-deny 供应链门）；`.github/`（PR 模板、issue 模板+安全引导、`CODEOWNERS`、`dependabot.yml`）；`docs/technical/release-process.md`（SemVer/versionCode 规则/tag/签名/发布 runbook）、`docs/technical/coding-standards.md`；CI 加 `supply-chain`(cargo-deny) job；README 加贡献/安全/许可三节。**配置集中化 + 推送 GitHub（D-018）**：新增根级 `project-config.toml` 作可配置项（署名/联系/仓库/维护者/应用 ID/版本）的单一真相来源，文档改为指向它。**已按用户要求推送到 GitHub：`git@github.com:Dongxp-lw/keyinput`（分支 main），提交身份 `linc <2250479723@qq.com>`（仓库本地配置；初始提交已改写为该身份）**；Dependabot 已自动开依赖更新 PR 分支（含 AGP 9.2.x，与 D-013 冲突，待用户处置）。2026-06-27。
- **运行期验证续（API 36，2026-06-27）——改条目/生物识别通过；导出/导入缺陷已修复并验证**：用 `uiautomator dump`（文本，免截图；App 普通界面可读，IME 窗口才需截图）驱动。①**改条目+落盘**：详情→编辑→改标题 GitHub2→GitHubEdited→保存→列表持久化；再**锁定→生物识别解锁**（从磁盘重开库）后标题仍为 GitHubEdited，**证编辑已写入 `vault.pivault`**。②**生物识别解锁（L4-BIO）端到端通过**：解锁屏「用生物识别解锁」→系统 BiometricPrompt（标题/「Fingerprint sensor」/「用主密码」兜底，id=button_negative）→`adb emu finger touch 1`→解锁回列表（两次复现）；设置页显「已启用。下次锁定后可用指纹/人脸快速解锁」。③**发现缺陷：导出/导入不可用（自动锁定与 SAF 冲突）**——导出走 SAF `CreateDocument`（默认名 `vault-backup.pivpkg`，已成功唤起 DocumentsUI 选择器并保存到 Downloads），但系统选择器使 App 进入后台→`VaultApplication.onStop` 触发**自动锁定**→选择器返回后 `exportTo` 调 `manager.exportPackage` 时会话**已锁定**→失败，文件**0 字节**且被弹回解锁屏。**导入同理**（同一机理：`onStop→lock()`）。**根因经源码确认**（`VaultApplication.onStop` 无条件 `lock()`；`SettingsScreen` 用 `rememberLauncherForActivityResult(CreateDocument/OpenDocument)`，回调里才执行 export/import）。**已修复并验证（2026-06-27）**：在 `VaultApplication` 加一次性 `suppressNextBackgroundLock` 标志，开自家导出/导入 SAF 选择器前置位、`onStop` 放行一次即复位（QR 扫码/BiometricPrompt 在本进程不触发 onStop，无需放行）。`assembleDebug` 绿。重装实测：导出→Downloads/vault-backup.pivpkg **1094B**（CBOR `TransferPackage`+magic `private-input-vault:transfer`，**无明文** GitHubEdited/种子/GitHub）、App 留在设置页显「已导出加密备份」；改标题 Changed1 → 导入该包 → 列表还原为 GitHubEdited、TOTP 实时码 `727660` 与独立 HMAC-SHA1（counter=59418560）逐位一致（**证种子逐位还原**）；导出/导入全程 App 不被弹回解锁屏。④**删除条目+落盘**：详情→删除→确认（软删除提示「可在下次同步前撤销，本地立即隐藏」）→列表空「还没有条目」+「已删除」；锁定→生物识别解锁后仍空（证删除已落盘）；再导入备份→GitHubEdited 还原（证导入可恢复到删空状态）。⑤**二维码扫描集成**：编辑→TOTP 种子框「扫码导入」→系统 CAMERA 运行时授权弹窗→zxing `CaptureActivity` 启动相机预览显「对准 2FA 二维码」（焦点 `com.lincdkeyinput/...CaptureActivity`，本进程不触发 onStop、不误锁）；取消→回编辑页仍解锁。**离开 App 到系统设置则正确自动锁定**（佐证修复只豁免 SAF，不影响真实后台锁定）。**未验证**：剪贴板 30s 实际清除（API 36 adb 读不了剪贴板：无 `cmd clipboard`、`dumpsys clipboard` 空）、二维码实际解码（无法给虚拟相机喂码）、真机抓包。2026-06-27。
- **安全键盘 IME 运行期验证（API 36，2026-06-26）——端到端通过（含 `commitText` 直填，TOTP 逐位正确）**：先前"键盘显示无法定论"已**推翻并更正**。**关键修复**：`adb shell ime reset` + `adb shell settings put secure show_ime_with_hard_keyboard 1` 后，软键盘恢复显示（此前是 AVD 硬件键盘模式抑制所有软 IME，非本 IME 缺陷）。随后逐项实测（截图取证 s24–s39，**`uiautomator dump` 无法捕获 IME 窗口——IME 必须以截图为准**）：①**自绘键盘渲染**：「🔒 安全键盘」头 + 「输入主密码解锁」+ 全 QWERTY/数字/符号键盘（s24）；②**自绘键输入登记**：点键出遮蔽圆点（s35 = 12 点）；③**Shift→大写**：点 ⇧ 显 `⇧●` 且字母转大写 `Q W E R…`（s34，早前漏点 ⇧ 导致输成小写口令被拒）；④**独立 Argon2id 解锁**：错口令→「主密码错误，或数据已被篡改」（证密码学路径真跑且正确拒绝，s28/s30/s36-err）；正确口令 `Passw0rd1234`→进 LIST 模式列条目（s36）；⑤**选条目→字段**：LIST→DETAIL 显「填入：动态验证码」（s37）；⑥**`commitText` 直填**：点字段把 TOTP 码直填进第三方输入框（App 搜索框），**全程不过系统剪贴板**（s38=`514798`、s39 续填）；⑦**直填 TOTP 逐位正确**：续填码 `581456`，独立 HMAC-SHA1 算 counter=59418329（epoch 1782549882）= `581456` **逐位一致**。**注**：IME 持有自己的 `VaultManager`（独立于 App 单例），App 解锁不解锁 IME（已实证：App 已解锁时 IME 仍 LOCKED，需各自解锁）——符合设计。已 `ime reset` 还原默认输入法。2026-06-26。
- **运行期冒烟续测（API 36，2026-06-26）——日常使用流程通过**：续上一轮，adb/input/screencap 实测。①**条目 CRUD**：FAB→新建条目→标题「GitHub」+字段→字段类型下拉（全 FieldKind 渲染）→选「验证码(TOTP)」（参数子表单：算法/位数/周期/发行方/账户+扫码图标）→保存→详情→返回列表持久化显「登录·1 个字段」；②**TOTP 实时码正确**：RFC 6238 种子（Base32 `GEZDG…`），详情显 `610492`+倒计时，**独立 HMAC-SHA1 算同窗口 T=59416090 = `610492` 逐位一致**（运行期密码学正确，非仅 KAT）；③**复制（L4-CLIP）**：Android 剪贴板预览**遮蔽为 `••••••`（敏感标记生效）**+「约 30s 自动清除」提示，TOTP 码不入 logcat（0 次）；④**搜索**：负例 zzz→「没有匹配的条目」、正例 Git→GitHub。**仍未运行期验证**：改/删条目、剪贴板 30s 实际清除、导出/导入 SAF、生物识别启用+解锁、安全键盘第三方直填、二维码扫描。2026-06-26。
- **运行期冒烟实测（API 36 模拟器，2026-06-26）——核心流程端到端通过**：用 `adb`/`input`/`screencap` 实际驱动。①安装 63MB APK（4 ABI .so+zxing）→启动 MainActivity，**native 库加载无 UnsatisfiedLinkError、无崩溃**，Compose 渲染引导页；②创建保险库：强度条实时显「强」、两次一致校验、按钮启用、**Argon2id KDF** 后 `vault.pivault`（343B）落盘→进入已解锁列表；③**锁定→解锁**：点锁→解锁屏（无生物识别入口，符合未启用）→输正确口令→**Argon2id open→回列表（证加解密往返在真机成立）**；④**隐私**：主密码在 5827 行 logcat **0 次出现**（MVP-010 运行期通过）；⑤**StrictMode 实触发**（捕获 `ViewModel.init` 主线程磁盘读，penaltyLog）。**发现**：(a) 引导页 `**无法找回**` 显示字面星号（Compose Text 不解析 Markdown）——已改为「无法找回」（源码修复，本次未重新部署）；(b) 轻微：`ViewModel.init` 的主线程文件读被 StrictMode 标记，建议后续移后台。**本次冒烟未覆盖**（仍为编译完成、运行期未验证）：增改条目/搜索、TOTP 实时码、复制自动清除、导出/导入 SAF、生物识别启用+解锁、安全键盘启用+第三方直填、二维码扫描。2026-06-26。
- **L0-05e CI：workflow 已写 + 本地门验证（真实 GitHub runner 未验证）**：新增 `.github/workflows/ci.yml`，3 个 job——**rust-core**（rustfmt 建议性 + `clippy -D warnings` + `cargo test`-KAT）、**android**（NDK r27 + cargo-ndk + `:app:assembleDebug` + Android lint 建议性）、**security-gates**（启发式日志扫描禁 `Log.*`/`println`/`println!`/`dbg!` 等 + 源码无 `INTERNET` 权限校验）。本地已复现可复现的门：security-gates 三项全 0、clippy 净、`cargo test` 88/88。**诚实说明**：① 整个 workflow **未在真实 runner 上跑通**（无 CI 环境），android job 依赖 runner 的 NDK/cargo-ndk 环境；② `rustfmt` 因**存量代码从未做过一次性 `cargo fmt`**（连旧的 `seal`/测试都有 diff）暂作建议性，待一次性格式化后转阻断；③ detekt/ktlint 待接入（workflow 内留 TODO）。2026-06-26。
- **L6-02 离线/隐私验证：静态/代码层完成（运行期待验证）**：① 源码与打包清单均**无 `INTERNET` 权限**（仅 CAMERA + biometric 既有 + androidx 自权限；用精确正则 `<uses-permission[^>]*INTERNET` 核对不存在，避免被自有注释误报）；② **代码零日志**——全仓 `grep` 确认 Rust 核心与 Android Kotlin 都没有 `println!`/`eprintln!`/`dbg!`/`log::`/`Log.*`/`Logger`，从根上杜绝主密码/密钥/字段值/TOTP 种子/验证码入日志；③ 开发构建启用 `StrictMode`（`VaultApplication.onCreate`，`buildConfig=true`，`detectAll`+`penaltyLog`，仅 DEBUG）；④ 纵深加固 `android:usesCleartextTraffic="false"` + `res/xml/network_security_config.xml`（`cleartextTrafficPermitted=false`），打包清单已含。`:app:assembleDebug` 绿。**运行期未验证（需设备）**：真机抓包零流量、logcat 实扫无敏感串、StrictMode 实触发。2026-06-26。
- **L6-01 核心已知答案测试（KAT）完成（标准向量逐字节通过）**：在 `core/vault-core/src/crypto.rs` 加 5 个 KAT，向量全部取自**权威来源并交叉核对**（非自洽生成）。**Argon2id**=RFC 9106 §5.3（tag `0d640df5…6b01e659`，经 argon2 crate 自带 KAT 二次确认）；**XChaCha20-Poly1305**=draft-arciszewski-xchacha-03 §A.1（**经我们的 `open` 解密标准密文得标准明文**，最强的互通证明）；**HKDF-SHA256**=RFC 5869 Test Case 1/3（TC3 的 `salt=空` 与 `derive_subkey` 的 `salt=None` 等价）；另加 `derive_kek`/`derive_subkey` 接线检验（证明封装层正确传参到 RFC 原语）。**TOTP**=RFC 6238 附录 B 12 向量此前已在 `totp.rs` 覆盖。`cargo test -p vault-core --lib` **88/88**（+5），clippy `-D warnings` 净；KAT 为 `#[cfg(test)]`，不进入 cdylib/arm64 release。2026-06-26。
- **TOTP 二维码扫描（D-014，编译+清单验证；运行期待验证）**：用户 2026-06-26 决定二维码扫描纳入 MVP。引入 `CAMERA` 运行时权限（camera `uses-feature` `required=false`）+ `zxing-android-embedded 4.3.0`（Apache-2.0、离线、不依赖 GMS）。`EntryEditScreen` 的 TOTP 种子框加扫码按钮：检查/运行时申请相机权限→`ScanContract` 扫码→`OtpAuth.parse` 复用粘贴解析路径回填全部参数。**已实查打包清单：仅新增 `CAMERA`（+biometric 既有权限），未被动引入 `INTERNET`，离线保证不破**。`:app:assembleDebug` 绿。二维码另一用途（v0.3 跨设备配对，承载短时配对密钥）不在本次范围。2026-06-26。
**MVP-002**：① 主密码强度提示——新增 `app/ui/PasswordStrength.kt`（长度×字符类别启发式分级 弱/中/强 + `PasswordStrengthBar`），接入创建（Onboarding）与改主密码（设置）；② 解锁失败计数 + 退避（LOCK-06）——新增 `vault-data/UnlockThrottle.kt`（失败次数与"下次可尝试时间"**持久化到 filesDir，杀进程/重启后仍生效**；前 4 次不退避，第 5 次起指数退避 5s→封顶 5min，成功即清零），`VaultViewModel.unlock` 改为节流版（仅对 `WrongPasswordOrTampered` 计数，不区分口令错/篡改），解锁界面显示失败次数 + 退避倒计时并禁用按钮。**MVP-007**：③ TOTP 参数可调——`EntryEditScreen` 的 `EditFieldState` 扩展 issuer/accountName/algorithm/digits/period，TOTP 字段展开子表单（算法 SHA-1/256/512、位数 6/8、周期秒、发行方、账户名），`buildEntry` 用用户所填参数（之前写死 SHA1/6/30）；④ otpauth:// 导入——新增 `vault-data/OtpAuth.kt`（解析 `otpauth://totp/...`），在 TOTP 种子框粘贴链接即自动回填全部参数；**仅文本粘贴解析，不引入相机/二维码（保持最小权限，扫码留后续单独决策）**。`:app:assembleDebug` 绿。2026-06-26。
- **L4 Android MVP 全部落地（编译验证；运行期待验证）**：在 `apps/android` 落地完整 MVP。新增共享桥接模块 **`:vault-data`**（`VaultManager` 组合 `VaultCore`+`VaultStore` 原子写盘；`Base32`；`ClipboardHelper` 敏感标记+约 30s 自动清除；`BiometricGate`）。**`:app`**（Compose/Material3）：`VaultApplication`（ProcessLifecycle 后台自动锁定）、`VaultViewModel`（状态机+动作，`VaultException`→中文提示，口令 `ByteArray` 用后清零）、引导/解锁/列表+搜索/详情(揭示·复制·TOTP 实时倒计时)/编辑+密码生成器/设置(改主密码+SAF 导出导入+生物识别开关)。**L4-BIO**：`BiometricGate`（Android Keystore AES-GCM、`setUserAuthenticationRequired`+`setInvalidatedByBiometricEnrollment`、`BiometricPrompt`+`CryptoObject` 包裹主密码落盘；新注册生物识别→密钥失效→自动降级主密码）。**L4-KBD**：`:keyboard` 模块 `VaultImeService`（**独立会话**的 `VaultManager`；自绘内置键盘输入主密码解锁，不依赖其它输入法、不过系统剪贴板；选条目→选字段→`commitText` 直填目标输入框；TOTP 字段实时算码后填入；空闲 60s/销毁自动锁定）。`:app:assembleDebug` 绿（含 4 ABI .so 重生成 + 绑定重生成 + IME service 合并进打包清单，含 `BIND_INPUT_METHOD` 与 `android.view.InputMethod` intent-filter）。**注：以上均为编译/清单层验证，端到端运行期未验证（无设备冒烟）**。2026-06-26。
- **L2-06 落地（`VaultCore` FFI 表面，接真实核心）**：新增 `core/vault-core/src/ffi.rs`。`VaultCore`（`uniffi::Object` + `Mutex<Option<Session>>`）导出：create/unlock/lock/is_unlocked/save/change_password、list_entries/get_entry/search/upsert_entry/delete_entry/get_field_value、export_package/import_package、touch/is_idle_expired；独立函数 generate_password/totp_now/inspect_package。FFI DTO 用 `uniffi::Record`；5 个领域枚举加 `uniffi::Enum`；VaultError 加 `Locked`。口令走 `Vec<u8>`→Kotlin `ByteArray`、用后 zeroize；**upsert 按 id 保留条目/字段/totp 级未知字段**（跨 FFI 前向兼容，含专门测试）；时间由平台注入。为 FFI 新增 `Session::create/from_vault`、`Vault::replace_content`、`VaultError::Locked`，并复用 `vault::CryptoProfile`。重生成 Kotlin 绑定含 `class VaultCore`（全方法）+ DTO `data class` + 枚举 + 顶层函数 → 验证「绑定生成、可调」。7 新测试（总 83/83），clippy `-D warnings` 净，`cargo ndk -t arm64-v8a build --release` 通过。2026-06-25。
- **L3-IMEX 落地（加密导出/导入，L3 收尾）**：新增 `core/vault-core/src/imex.rs`。`TransferPackage{header, wrappedPackageDek, payload}`，头部含 magic/packageVersion/schemaVersion/createdAt/sourceDeviceLabel?/vaultId/cryptoProfile（**作 AAD**）。`export/export_with_params`（独立 salt + 随机 package DEK）、`inspect`（仅解析头部门禁，无需口令）、`import`。链路：Argon2id package KEK → 信封解包 package DEK → HKDF **`SUBKEY_MIGRATION`**（与本地内容密钥域分离）→ XChaCha20-Poly1305。复用 VAULT 的 `CryptoProfile`（已改 pub）。错口令=篡改=`WrongPasswordOrTampered`，损坏=`Corrupt`，版本不兼容=`IncompatibleVersion`；`import` 无副作用（失败不动现有库，原子写入归平台）；明文用后 zeroize。含导出不可明文读测试（TP-105）。8 新测试（总 76/76），clippy `-D warnings` 净，`cargo ndk -t arm64-v8a build --release` 通过。2026-06-25。- **L3-TOTP 落地（RFC 6238 验证码）**：新增 `core/vault-core/src/totp.rs`。`TotpParameters`/`TotpCode` + `generate(params, now)`：HOTP 动态截断（RFC 4226）+ 时间步 floorDiv + 8 字节大端计数器（支持 2038 后大于 32 位），HMAC-SHA1/256/512 用新增 `hmac 0.13`+`sha1 0.11`（与现有 `sha2 0.11` 同为 digest 0.11）；时间由上层注入；digits∈6/8、period>0、种子≥16B 校验（不静默回退）；中间缓冲 zeroize。**RFC 6238 附录 B 全 12 向量通过**（三算法×4 时间，含 20000000000=2603 年验证 64 位时间步）。4 新测试（总 68/68），clippy `-D warnings` 净，`cargo ndk -t arm64-v8a build --release` 通过。注：hmac 0.13 需导入 `KeyInit` trait 才有 `new_from_slice`。2026-06-25。
- **L2-05 + L3-GEN 落地（采样 + 密码生成器）**：新增 `core/vault-core/src/rng.rs`（`uniform_index(bound)` 拒绝采样，无溢出计算 `2^32 mod bound`，避免取模偏置）与 `generator.rs`（`PasswordPolicy` + `generate` 返回 `Zeroizing<String>`，Drop 清零；字符集组装、可排除易混淆 `O0lI1`、至少一类与长度下限 `MIN_LENGTH=8` 校验，映射用拒绝采样无偏置，不记日志）。10 新测试（总 64/64，含 rng/generator 两个分布无偏置检验），clippy `-D warnings` 净，`cargo ndk -t arm64-v8a build --release` 通过。2026-06-25。
- **L3-LOCK 落地（解锁会话，核心侧）**：新增 `core/vault-core/src/lock.rs`。`Session::unlock(bytes, pw, now)` 用 `Vault::open` 建会话（错口令=篡改 不可区分）；**最小持有**（仅 DEK）；`touch`+`is_idle_expired` 纯空闲判定；`lock(self)` 与 `Drop` 都调 `VaultContent::zeroize_secrets`（清零字段明文 + TOTP 种子；加了 `EntryRepository::content_mut`），DEK 随 Vault 的 SecretKey 清零；遮蔽 Debug。平台侧归 L4。6 新测试（总 54/54），clippy 净，arm64 通过。2026-06-25。
- **L3-VAULT 落地（保险库加密核心）**：新增 `core/vault-core/src/vault.rs`。文件格式 `VaultFile{header, wrappedDek, payload}`，头部 `VaultHeader{magic, formatVersion, schemaVersion, cryptoProfile{kdf,aead}}` **作 AAD**（`serde_bytes` 编码字节串）。`Vault::create/create_with_params`（后者注入 KDF 快参）、`open`（先格式/版本门禁再密码学）、`save`（每次新 nonce）、`change_password`（仅重包装 DEK，salt 不变）。链路：Argon2id KEK → 信封解包 DEK → HKDF 内容子密钥 → XChaCha20-Poly1305 加密 `VaultContent` CBOR。错口令=篡改=`WrongPasswordOrTampered`，损坏=`Corrupt`，不兼容=`IncompatibleVersion`；明文用后 `zeroize`；`Vault` 遮蔽 Debug。11 测试（总 48/48），clippy 净，arm64 通过。标准 KAT 向量留 L6-01。2026-06-25。
- **L3-ENTRY 落地（条目/字段模型）**：新增 `core/vault-core/src/entry.rs`（`Entry`/`Field`/`TotpField`/`VaultContent`/`Settings` + 枚举 + 默认策略 `FieldKind::default_policy`（§6）+ `VaultContent::to_bytes/from_bytes`）与 `repository.rs`（`EntryRepository`：upsert/get/list/soft_delete + 本地 `search`）。未知字段经 `#[serde(flatten)]` 保留（含向 Entry CBOR 注入未来字段的保留测试）；搜索只覆盖标题/标签/字段标签与**普通敏感度**字段值；时间由上层注入。新增 13 测试（总 37/37），clippy 净，arm64 通过。注：含 `UnknownFields` 的结构不能 derive `Eq`（`ciborium::Value` 含 f64）。2026-06-25。
- **L2-04 落地（错误模型 / 跨 FFI）**：新增 `core/vault-core/src/error.rs`，定义对外统一错误 `VaultError`（`WrongPasswordOrTampered`/`Corrupt`/`IncompatibleVersion`/`InvalidInput`/`Internal`）+ `VaultResult<T>`；`From<CryptoError>`/`From<CodecError>` 收敛内部错误，**口令错与篡改都映射到同一个 `WrongPasswordOrTampered`（不可区分，安全要求，含专门测试）**；`#[derive(uniffi::Error)]` 导出，重生成的 Kotlin 绑定含 `sealed class VaultException`（5 变体 + `FfiConverterTypeVaultError` + `ErrorHandler`）→ 验证「经 UniFFI 正确映射」。新增 6 测试，`clippy -D warnings` 净，`cargo ndk -t arm64-v8a build --release` 通过。2026-06-25。
- **L2-03 落地（序列化）**：新增 `core/vault-core/src/codec.rs`，使用 `ciborium`（CBOR）+ `serde` 实现通用编解码（`to_cbor`/`from_cbor`）；通过 `#[serde(flatten)] + BTreeMap<String, Value>` 保留未知字段（跨版本前向兼容，D-005）。新增 5 个测试（简单往返、跨版本未知字段保留、同 schema 无未知字段、未知字段编码确定性、不兼容/截断拒绝），`cargo test -p vault-core --lib` 总计 18/18 通过；`cargo clippy -p vault-core --lib --tests -- -D warnings` 通过；`cargo ndk -t arm64-v8a build -p vault-core --release` 通过。2026-06-25。
- **L2-01 / L2-02 落地（加密核心）**：`core/vault-core/src/secret.rs`（`SecretKey`/`SecretBytes`：`zeroize` 清零 + 遮蔽 `Debug` + `subtle` 常量时间比较）、`crypto.rs`（Argon2id KEK / XChaCha20-Poly1305 seal·open / 信封 wrap·unwrap / HKDF-SHA256 子密钥），全用审计过的 RustCrypto crate（D-004/D-008）；13 测试通过、clippy `-D warnings` 净、arm64 交叉编译通过。标准 KAT 向量留 L6-01。2026-06-25。
- **L0-05d 落地（完整链路验证）**：搭 Gradle 多模块工程（AGP 8.7.3/Gradle 8.9/Kotlin 2.1.0/compileSdk 35）+ wrapper；cargo-ndk/uniffi-bindgen 接为增量 Gradle 任务；`:app:assembleDebug` 绿（APK 含 4 ABI .so）；JNA 用 `@aar`；API 36 模拟器上 `PingInstrumentedTest` 通过（Kotlin→Rust 运行期往返）。2026-06-24。
- **L0-05c 落地**：装 NDK r27d（SHA-1 校验）+ 4 个 Rust Android target + `cargo-ndk 4.1.2`；交叉编译出 4 个 ABI 的 `libvault_core.so` 入 core-bindings/jniLibs；`uniffi-bindgen` 生成 Kotlin 绑定入 core-bindings。Gradle 工程与 `assemble` 归入 L0-05d。2026-06-24。
- **L0-05b 落地**：导出平凡函数 `ping` + Rust 测试通过；`uniffi-bindgen`（feature 门控 `bindgen`，保持 cdylib 精简）生成 Kotlin 绑定（`fun ping(...)`）。Kotlin 运行期 FFI 调用留到 L0-05d（本机无 kotlinc）。2026-06-24。
- **L0-05a 落地**：Cargo 工作区 + `vault-core` crate（lib+cdylib）+ UniFFI 空脚手架（`uniffi 0.31.2`）；`cargo build` 通过（2026-06-24）。
- 安装并验证 Rust 工具链（gnu host）+ WinLibs MinGW（`dlltool`）。
- 开源组件选型调研，写入 [技术架构](../docs/technical/architecture.md) §2 + [安全实现设计](../docs/technical/security-implementation-design.md) §2.4。
- 11/11 实现文档。

## 来源指引

- 计划：[execution-plan.md](execution-plan.md)；任务：[tasks.md](tasks.md)；决策：[decisions.md](decisions.md)。
