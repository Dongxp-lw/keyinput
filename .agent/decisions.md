# 决策登记（ADR-lite）

状态：LOCKED（已锁定）/ PENDING（待定）。改 LOCKED 决策须在此更新并记原因，避免反复推翻。

| ID | 决策 | 状态 | 依据/来源 |
| --- | --- | --- | --- |
| D-001 | 平台优先级：Android 优先；iOS/HarmonyOS 放到 v1 末尾 | LOCKED | 产品决策记录基线 |
| D-002 | 进入自顶向下分层构建；用户要求先把架构再细化一层（共享核心/跨平台），**暂不写代码** | LOCKED | 用户 2026-06-24 |
| D-003 | 加密方案：Argon2id 派生 KEK + 随机 DEK 信封 + XChaCha20-Poly1305 + 头部作 AAD | LOCKED | [安全实现设计](../docs/technical/security-implementation-design.md) §2-4 |
| D-004 | 加密：选 Rust 共享核心后，加密原语用 Rust crate（RustCrypto 的 argon2+chacha20poly1305，或 libsodium-sys）；不用 lazysodium-android。**落地（2026-06-25）：argon2 0.5.3 + chacha20poly1305 0.10.1 + hkdf 0.13 + sha2 0.11 + zeroize 1.9 + subtle 2.6 + getrandom 0.2，已实跑+测试+arm64 交叉编译** | LOCKED | 用户 2026-06-24、§2.4、L2-02 实测 |
| D-005 | 序列化（Rust 核心）：需保留未知字段（跨版本前向兼容，安全实现设计 §4）。**已锁定采用 `ciborium`（CBOR）+ `serde` `#[flatten]` 捕获额外字段**；未知字段容器采用 `BTreeMap<String, ciborium::Value>` 确保重编码稳定。**`prost`（protobuf）会丢未知字段，故不选**。 | LOCKED | 安全实现设计 §4、数据模型 §8、L2-03 实测（未知字段保留测试 + clippy + arm64 Android 交叉编译） |
| D-006 | 架构：**一套 Rust 共享核心 + 各端原生 UI**（Android/iOS 经 UniFFI，HarmonyOS 经 Node-API）；HarmonyOS Rust 工具链待 spike | LOCKED | 用户 2026-06-24、[notes/shared-core-options.md](notes/shared-core-options.md) |
| D-007 | 本地传输：Nearby 依赖 GMS，须 Wi-Fi Direct/文件包回退 | LOCKED | 架构 §2 选型 |
| D-008 | 不自行实现密码学原语，只在审计过的库间选择 | LOCKED | 安全要求、§2.4 |
| D-009 | Agent 工作区文件夹采用 `.agent/` | LOCKED | 用户 2026-06-24 |
| D-010 | 商业模式：免费层保证正常使用（密码、2FA/TOTP、本地保险库、导出与手动迁移）；增值（云同步多端共享、键盘皮肤等）账号绑定、延后 MVP、不影响正常使用 | LOCKED | 用户 2026-06-24、[版本与权益计划](../docs/product/version-plan.md) |
| D-011 | SDK 基线：**minSdk = 28**（Android 9，安全优先）；compileSdk/targetSdk 取最新稳定，工具版本落地时锁定 | LOCKED | 用户 2026-06-24、[工程基础](../docs/technical/engineering-foundation.md) §4 |
| D-012 | 应用 ID **暂定 `com.lincdkeyinput`**（工作值，发布前定稿）；最终品牌/显示名待产品成形后定；产品**不**整体改名，文档仍称 Private Input Vault | PROVISIONAL | 用户 2026-06-24、[工程基础](../docs/technical/engineering-foundation.md) §5.1 |
| D-013 | Android 构建工具链（**已实跑验证** 2026-06-24）：NDK r27d (27.3.13750724) 经 cargo-ndk → 4 ABI `.so`；Gradle 工程 = **AGP 8.7.3 + Gradle 8.9 + Kotlin 2.1.0 + compileSdk 35 + JDK 17/21**（用现成 android-35，避开装 cmdline-tools/android-36）；UniFFI Kotlin 运行期 = **JNA `5.17.0@aar`**（@aar 必需，否则设备上缺 libjnidispatch.so）；预编译 `.so` 经 jniLibs 打包，AGP 不调 NDK；cargo-ndk/uniffi-bindgen 接为 Gradle 任务。`:app:assembleDebug` + 模拟器 `connectedDebugAndroidTest` 均绿 | LOCKED | 实跑验证 2026-06-24（放弃早前拟用的 AGP 9.2 以免多装 SDK） |
| D-014 | TOTP 二维码扫描纳入 **MVP**（用户 2026-06-26 要求）：引入 **`CAMERA` 运行时权限**（`uses-feature` camera 设为 `required=false`，无相机设备仍可装+用粘贴导入）；二维码库选 **`com.journeyapps:zxing-android-embedded 4.3.0`**（Apache-2.0、纯离线、**不依赖 GMS**，与 MIGR 既定 ZXing 选型一致，不引第二套库）。扫码结果走 `OtpAuth.parse` 复用粘贴解析路径。**已验证打包清单未被动引入 `INTERNET` 权限**（离线保证不破）。otpauth 导入由此前的"待确认"转为 MVP 内（粘贴+扫码两路） | LOCKED | 用户 2026-06-26、架构 §2 ZXing 选型、打包清单实查 |
| D-015 | **网络使用原则**（用户 2026-06-26）：产品默认完全离线、**不声明 `INTERNET` 权限**。仅当某功能**确实需要互联网**时，才在该功能落地时声明 `INTERNET`，并在运行时**显式告知用户"此功能需要联网"再使用**；本地核心功能永不依赖网络。**技术澄清：TOTP/2FA 是纯本地计算（RFC 6238），不需要联网**（我方为生成方=离线；验证在对方服务器）。盘点：v0.2 MVP 与 v0.3 迁移（MIGR，本地传输，[cross-device-migration §… 不声明 INTERNET](../docs/implementation/cross-device-migration.md)）**均零互联网**；**唯一需要互联网的功能 = 云同步 SYNC（v1.1）**，届时按本原则申请权限+告知用户 | LOCKED | 用户 2026-06-26、[cloud-sync.md](../docs/implementation/cloud-sync.md)、[cross-device-migration.md](../docs/implementation/cross-device-migration.md) |
| D-016 | **开源许可证 = Apache License 2.0**（用户 2026-06-27 决定开源，并要求"不对其他版本的作品负责"）。Apache-2.0 最匹配：宽松开源 + 明确**免责声明/责任限制**（§7–8 "AS IS"）+ **修改版须显著标注改动**（§4(b)，故原作者不为第三方修改/衍生版背书）+ 专利与商标条款；与本项目依赖（RustCrypto MIT/Apache、AndroidX/ZXing Apache-2.0、UniFFI MPL-2.0）兼容。已建 `LICENSE`、`NOTICE`（第三方组件声明，以构建清单为权威来源）。版权行用占位 `The Private Input Vault Authors`（可改）。**MIT 也免责但无"标注改动"机制，故选 Apache-2.0** | LOCKED | 用户 2026-06-27 |
| D-017 | **工程治理规范集（主流做法）**（用户 2026-06-27 要求补齐并可加其它规范）：版本号 **SemVer**；提交 **Conventional Commits** + **DCO sign-off**；分支 `main` + 短期特性分支 + squash merge。新增文件：`CONTRIBUTING.md`、`SECURITY.md`（私密漏洞披露）、`CODE_OF_CONDUCT.md`（Contributor Covenant 2.1）、`CHANGELOG.md`（Keep a Changelog）、`AGENTS.md`、`.editorconfig`、`deny.toml`（cargo-deny 供应链门）、`.github/`（PR 模板、issue 模板、`CODEOWNERS`、`dependabot.yml`）、`docs/technical/release-process.md`（发布 runbook+版本号策略）、`docs/technical/coding-standards.md`；CI 加 `supply-chain`(cargo-deny) job。占位项（发布前替换真实值）：安全/行为准则联系邮箱、`CODEOWNERS` 团队名、GitHub 仓库 URL | LOCKED | 用户 2026-06-27 |
| D-018 | **托管与配置集中化**（用户 2026-06-27）：① 远端 = **`git@github.com:Dongxp-lw/keyinput`（默认分支 main）**，提交身份 **`linc <2250479723@qq.com>`**（仓库本地 git 配置，未改全局；初始提交已一并改写为该身份，避免泄露公司邮箱）。**仍遵守“非经用户要求不擅自 commit/push”**。② 全项目**可配置项集中到根级 [project-config.toml](../project-config.toml)**（署名/联系/仓库/维护者/应用 ID/版本；**绝不写秘密**），文档指向它。③ `.github/` 协作脚手架已随推送生效（Dependabot 已自动开依赖更新 PR 分支，含 AGP 9.2.x——与 D-013 冲突，待处置）。**注：早前“仅本地 git、暂不推送”已由用户 2026-06-27 改为推送至 Dongxp-lw/keyinput** | LOCKED | 用户 2026-06-27 |
